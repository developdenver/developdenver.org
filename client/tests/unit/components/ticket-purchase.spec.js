describe('ticket-purchase', () => {
    it('prefills the logged-in user when they haven\'t yet purchased a ticket', () => {

    });
    it('omits the logged-in user email when they are already an attendee', () => {

    });

    it('updates the number remaining tickets', () => {

    });
});